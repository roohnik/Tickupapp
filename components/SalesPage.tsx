import React from 'react';

const SalesPage: React.FC = () => {
    return (
        <div className="flex items-center justify-center h-full text-gray-500">
            <h1 className="text-2xl font-semibold">صفحه فروش - به زودی...</h1>
        </div>
    );
};

export default SalesPage;
