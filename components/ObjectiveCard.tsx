// This component is no longer in use and has been replaced by ObjectiveRow for the list view
// and ObjectiveSidePanel for the detail view. Keeping the file to avoid breaking potential imports,
// but its content is removed.

import React from 'react';

const ObjectiveCard: React.FC<any> = () => {
  return null;
};

export default ObjectiveCard;
